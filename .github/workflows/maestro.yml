name: Maestro

# The mobile (Maestro) e2e suite has been retired and no flows remain.
# This workflow is inert and is pending deletion.

on:
  workflow_dispatch:

jobs:
  noop:
    name: No mobile e2e flows
    runs-on: ubuntu-latest
    steps:
      - run: echo "No mobile e2e flows to run."
