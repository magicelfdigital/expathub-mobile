PROMPT 1 — MOBILE AGENT (Expo / React Native)
Copy the entire block below and paste directly into the Replit mobile agent chat.

You are updating the ExpatHub mobile app (Expo SDK 54,
expo-router, React Native) as part of the v1.1 release.
I am providing all required asset files. Make all changes
exactly as specified. Confirm completion of each step
before proceeding to the next.

ASSETS PROVIDED — place in assets/images/:
  expathub-map-1x.png   (375x160px  — mobile 1x)
  expathub-map-2x.png   (750x320px  — mobile 2x)
  expathub-map-3x.png   (1125x480px — mobile 3x)

ICON LIBRARY: @expo/vector-icons (already installed).
Use Ionicons throughout. Do NOT change any existing
icon names or sizes — only update colors where needed.
Reference: import { Ionicons } from '@expo/vector-icons';

─────────────────────────────────────────
STEP 1 — COLOR CONSTANTS FILE
─────────────────────────────────────────
Create or replace constants/colors.ts with exactly:

export const colors = {
  blue:       '#3E81DD',  // Hub blue — primary buttons, pills
  teal:       '#33C4DC',  // Expat teal — active tabs, accents
  tealLight:  '#E8F9FC',  // Teal tint — table headers
  gold:       '#E8991A',  // Sun gold — Pathfinder, upgrade CTAs
  goldLight:  '#FEF4E2',  // Gold tint — badges, disclaimers
  cream:      '#EEEEE4',  // Logo bar background
  surface:    '#FFFFFF',  // Cards
  navy:       '#1C2B5E',  // Primary text on light surfaces
  textMid:    '#5A6785',  // Body text
  textSoft:   '#9BA8C0',  // Labels, meta text
  onDark:     '#FFFFFF',  // Text on dark map background
  onDarkMid:  'rgba(255,255,255,0.75)',  // Secondary on dark
  onDarkSoft: 'rgba(255,255,255,0.45)',  // Tertiary on dark
  cardBg:     'rgba(255,255,255,0.92)',  // Floating cards
  glassDark:  'rgba(15,25,60,0.65)',     // Nav bar glass
  glassLight: 'rgba(238,238,228,0.88)', // Logo bar glass
  border:     'rgba(62,129,221,0.11)',
  borderDark: 'rgba(255,255,255,0.12)', // Borders on dark bg
  shadow:     'rgba(0,0,0,0.25)',
};

Confirm: constants/colors.ts created. Do not proceed until confirmed.

─────────────────────────────────────────
STEP 2 — GLOBAL MAP BACKGROUND
─────────────────────────────────────────
The map image replaces the solid background color on ALL screens.
In the root layout (app/_layout.tsx or equivalent), wrap the
entire app in an ImageBackground:

  import { ImageBackground } from 'react-native';

  <ImageBackground
    source={require('../assets/images/expathub-map-1x.png')}
    style={{ flex: 1 }}
    resizeMode='cover'
  >
    {/* dark overlay */}
    <View style={{
      position: 'absolute', inset: 0,
      backgroundColor: 'rgba(15,25,60,0.50)',
    }} />
    {/* app content */}
    <Slot />
  </ImageBackground>

Expo handles @2x/@3x automatically. Reference only the 1x file.
All screen backgrounds should be transparent so the map shows through.

Confirm: global map background applied to all screens.

─────────────────────────────────────────
STEP 3 — LOGO BAR
─────────────────────────────────────────
Update the top navigation/header bar on ALL screens:
- Background: colors.glassLight (rgba(238,238,228,0.88))
- Apply blur effect: use expo-blur BlurView with intensity 20
  tint='light' if expo-blur is installed, otherwise use
  the glassLight color as a solid background
- Logo image: render at natural colors, no tinting
- Height: 46px content area
- Border bottom: 1px, color colors.borderDark
- On Pathfinder detail screens: show badge on right
  (text color gold #E8991A, background goldLight #FEF4E2,
   border rgba(232,153,26,0.25), borderRadius 20, padding 4/10)

Confirm: logo bar updated on all screens.

─────────────────────────────────────────
STEP 4 — FLOATING CARDS
─────────────────────────────────────────
All card components throughout the app:
- backgroundColor: colors.cardBg (rgba(255,255,255,0.92))
- borderRadius: 16
- borderWidth: 1, borderColor: rgba(255,255,255,0.50)
- shadowColor: '#000', shadowOffset: {width:0,height:4},
  shadowOpacity: 0.25, shadowRadius: 12, elevation: 8
- Text inside cards: navy #1C2B5E as before

Country cards, feature cards, stat cards — all use cardBg.

Confirm: all cards updated to floating style.

─────────────────────────────────────────
STEP 5 — ACTIVE TAB (FILLED TEAL PILL)
─────────────────────────────────────────
On all horizontal tab/filter bars:
- Active tab: backgroundColor colors.teal (#33C4DC),
  color white, borderRadius 20, paddingHorizontal 14,
  paddingVertical 7, fontWeight '600'
- Inactive tab: backgroundColor rgba(255,255,255,0.12),
  color rgba(255,255,255,0.60), borderRadius 20

Confirm: active tab updated to filled teal pill.

─────────────────────────────────────────
STEP 6 — COMPARISON TABLE HEADERS
─────────────────────────────────────────
On the comparison/table screen:
- Country column headers (flag + name cells):
  backgroundColor: colors.tealLight (#E8F9FC)
  borderRadius: top corners 10px only
  flag fontSize: 24, name fontWeight: '700', color: navy
- Row label column: transparent background
- Alternate data rows: backgroundColor rgba(62,129,221,0.03)
- Table container: use cardBg background, borderRadius 16,
  shadow as per Step 4

Confirm: comparison table headers updated.

─────────────────────────────────────────
STEP 7 — TEXT ON DARK BACKGROUND
─────────────────────────────────────────
Any text that sits directly on the map background
(NOT inside a card) must use light colors:
- Headlines: colors.onDark (white)
- Body/subtitle: colors.onDarkMid (rgba(255,255,255,0.75))
- Labels/meta: colors.onDarkSoft (rgba(255,255,255,0.45))
- Region labels above card lists: onDarkSoft

Text INSIDE cards keeps navy/textMid/textSoft as before.

Confirm: all on-dark text updated.

─────────────────────────────────────────
STEP 8 — BOTTOM NAVIGATION
─────────────────────────────────────────
Update the bottom tab bar:
- Background: colors.glassDark (rgba(15,25,60,0.65))
- Apply BlurView if expo-blur available, intensity 15, tint='dark'
- Border top: 1px, color colors.borderDark
- Active icon + label: colors.teal (#33C4DC)
- Inactive icon + label: colors.onDarkSoft
- Active indicator pip: 4x4px circle, colors.teal
- Ionicons: use filled variant for active, outline for inactive
  e.g. active: 'home' inactive: 'home-outline'

Confirm: bottom nav updated.

─────────────────────────────────────────
STEP 9 — TYPOGRAPHY
─────────────────────────────────────────
Install if not already present:
  npx expo install @expo-google-fonts/lora
  npx expo install @expo-google-fonts/dm-sans

Import in root layout:
  import { Lora_600SemiBold } from '@expo-google-fonts/lora';
  import { DMSans_400Regular, DMSans_500Medium,
    DMSans_600SemiBold, DMSans_700Bold }
    from '@expo-google-fonts/dm-sans';

Apply:
- Display headlines, country names, screen titles: Lora_600SemiBold
- All other text: DM Sans variants by weight

Confirm: fonts installed and applied.

─────────────────────────────────────────
STEP 10 — SEARCH BAR ON DARK
─────────────────────────────────────────
The search bar sits on the dark map background:
- backgroundColor: rgba(255,255,255,0.15)
- borderRadius: 12
- borderWidth: 1, borderColor: rgba(255,255,255,0.20)
- Placeholder text color: rgba(255,255,255,0.45)
- Icon color: rgba(255,255,255,0.50)

Confirm: search bar updated for dark background.

─────────────────────────────────────────
STEP 11 — FINAL CLEANUP
─────────────────────────────────────────
Search for all remaining hardcoded color values not in
colors.ts. Replace with appropriate color constants.
Ensure no screen has a solid opaque background that
would block the map showing through.

List every file modified across all steps.

─────────────────────────────────────────
DO NOT:
- Modify any Supabase queries or data logic
- Modify any RevenueCat or entitlement logic
- Modify any Stripe or payment code
- Auto-deploy or publish without being asked
- Change any database field names or API calls
- Change existing Ionicons icon names or sizes


PROMPT 2 — WEB AGENT (React)
Copy the entire block below and paste directly into the Replit web agent chat.

You are updating the ExpatHub web app (React) as part of
the v1.1 release. I am providing all required asset files.
Make all changes exactly as specified. Confirm completion
of each step before proceeding to the next.

ASSETS PROVIDED — place in public/images/ or src/assets/images/:
  expathub-map-web-1x.png   (1200x400px — standard)
  expathub-map-web-2x.png   (2400x800px — retina)

─────────────────────────────────────────
STEP 1 — COLOR CONSTANTS + CSS VARIABLES
─────────────────────────────────────────
Create or replace src/theme/colors.ts with:

export const colors = {
  blue:       '#3E81DD',
  teal:       '#33C4DC',
  tealLight:  '#E8F9FC',
  gold:       '#E8991A',
  goldLight:  '#FEF4E2',
  cream:      '#EEEEE4',
  surface:    '#FFFFFF',
  navy:       '#1C2B5E',
  textMid:    '#5A6785',
  textSoft:   '#9BA8C0',
  onDark:     '#FFFFFF',
  onDarkMid:  'rgba(255,255,255,0.75)',
  onDarkSoft: 'rgba(255,255,255,0.45)',
  cardBg:     'rgba(255,255,255,0.92)',
  glassDark:  'rgba(15,25,60,0.65)',
  glassLight: 'rgba(238,238,228,0.88)',
  border:     'rgba(62,129,221,0.11)',
  borderDark: 'rgba(255,255,255,0.12)',
  shadow:     'rgba(0,0,0,0.25)',
};

Add to global CSS (index.css / globals.css):

:root {
  --blue:        #3E81DD;
  --teal:        #33C4DC;
  --teal-light:  #E8F9FC;
  --gold:        #E8991A;
  --gold-light:  #FEF4E2;
  --navy:        #1C2B5E;
  --card-bg:     rgba(255,255,255,0.92);
  --glass-dark:  rgba(15,25,60,0.65);
  --glass-light: rgba(238,238,228,0.88);
  --border-dark: rgba(255,255,255,0.12);
}

Confirm: colors.ts and CSS variables created.

─────────────────────────────────────────
STEP 2 — GLOBAL MAP BACKGROUND
─────────────────────────────────────────
Add to global CSS:

body {
  background-image: url('/images/expathub-map-web-1x.png');
  background-image:
    image-set(
      url('/images/expathub-map-web-1x.png') 1x,
      url('/images/expathub-map-web-2x.png') 2x
    );
  background-size: cover;
  background-position: center top;
  background-attachment: fixed;
  min-height: 100vh;
}

Add a dark overlay div as the first child of the root app
container, positioned fixed, full screen:
  background: rgba(15,25,60,0.50);
  position: fixed; inset: 0; z-index: 0;

All page content sits above this overlay (z-index: 1+).

Confirm: global map background applied.

─────────────────────────────────────────
STEP 3 — LOGO BAR / NAV
─────────────────────────────────────────
Update the top navigation bar on ALL pages:
- Background: rgba(238,238,228,0.88) with backdrop-filter: blur(12px)
- Logo: natural colors, no filter
- Height: 60px
- Border bottom: 1px solid rgba(255,255,255,0.12)
- On detail pages: Pathfinder badge right-aligned
  (color #E8991A, background #FEF4E2, border-radius 20px,
  padding 4px 10px, border 1px solid rgba(232,153,26,0.25))

Confirm: nav bar updated across all pages.

─────────────────────────────────────────
STEP 4 — FLOATING CARDS
─────────────────────────────────────────
All card components:
- background: rgba(255,255,255,0.92)
- border-radius: 16px
- border: 1px solid rgba(255,255,255,0.50)
- box-shadow: 0 4px 20px rgba(0,0,0,0.25)
- backdrop-filter: blur(4px)
- Text inside cards stays navy/dark as before

Confirm: all cards updated.

─────────────────────────────────────────
STEP 5 — ACTIVE TAB (FILLED TEAL PILL)
─────────────────────────────────────────
Active tab: background #33C4DC, color white,
border-radius 20px, padding 7px 14px, font-weight 600.
Inactive tab: background rgba(255,255,255,0.12),
color rgba(255,255,255,0.60), border-radius 20px.

Confirm: tabs updated.

─────────────────────────────────────────
STEP 6 — COMPARISON TABLE HEADERS
─────────────────────────────────────────
Country column header cells:
- background: #E8F9FC
- border-radius: 10px 10px 0 0
- flag: font-size 24px
- name: font-weight 700, color #1C2B5E
Table container: card style (rgba(255,255,255,0.92))
Alternate rows: background rgba(62,129,221,0.03)

Confirm: table updated.

─────────────────────────────────────────
STEP 7 — TEXT ON DARK BACKGROUND
─────────────────────────────────────────
Text directly on the map (not inside cards):
- Headlines: color white
- Body/subtitle: color rgba(255,255,255,0.75)
- Labels/meta: color rgba(255,255,255,0.45)
Text inside cards: navy/textMid unchanged.

Confirm: on-dark text updated.

─────────────────────────────────────────
STEP 8 — TYPOGRAPHY
─────────────────────────────────────────
Add to <head> in index.html:
  <link href='https://fonts.googleapis.com/css2?
    family=DM+Sans:wght@400;500;600;700;800&
    family=Lora:wght@500;600&display=swap'
    rel='stylesheet'>

Global CSS:
  body { font-family: 'DM Sans', sans-serif; }
  h1, h2, .hero-headline, .country-name {
    font-family: 'Lora', serif; font-weight: 600;
  }

Confirm: fonts applied.

─────────────────────────────────────────
STEP 9 — SEARCH BAR ON DARK
─────────────────────────────────────────
Search bar (sits on dark background):
- background: rgba(255,255,255,0.15)
- border-radius: 12px
- border: 1px solid rgba(255,255,255,0.20)
- backdrop-filter: blur(4px)
- placeholder color: rgba(255,255,255,0.45)

Confirm: search bar updated.

─────────────────────────────────────────
STEP 10 — FINAL CLEANUP
─────────────────────────────────────────
Search for all remaining hardcoded colors not in the
new system. Replace with CSS variables or color constants.
Ensure no page element has an opaque background that
blocks the map from showing through.

List every file modified across all steps.

─────────────────────────────────────────
DO NOT:
- Modify any Supabase queries or data logic
- Modify any Stripe or payment logic
- Modify any subscription or entitlement code
- Auto-deploy without being asked
- Change any API calls or database field names
